#include<bits/stdc++.h>
#include"alist.h"
#include"alist_template.cpp"

#define MAXLEN 100

using namespace std;
int dataInput[MAXLEN];
//int dataAnswer[MAXLEN];
int k; // The index after which the elements in the list is supposed to be deleted

int main()
{
    freopen("data/input.in", "r", stdin);

    // First read the type to test
    string type;
    cin >> type;
    switch (type)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
    
    


    
    // Generate our answer
    

}